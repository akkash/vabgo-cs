/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'out',
    images:{
        unoptimized:true,
        domains:['mafekvxxizlcavhbkybi.supabase.co']
    },
};

export default nextConfig;
